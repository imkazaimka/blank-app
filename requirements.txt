# --- Core (always required) ---
streamlit>=1.31
numpy>=1.24
pandas>=2.0
plotly>=5.18
scipy>=1.10

# --- Optional: live Zebra reader over LLRP ---
# The app runs fully in Simulator mode without this; install it to stream real
# reads from an FX7500 / FX9600 / FXR90 reader.  Imported lazily.
#
#   sllurp>=1.2.1
#
# Install with:  pip install sllurp
