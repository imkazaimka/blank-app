# --- Core (always required) ---
streamlit>=1.31
numpy>=1.24
pandas>=2.0
plotly>=5.18
scipy>=1.10

# --- Optional: live Zebra reader ---
# The app runs fully in Simulator mode without these; install to stream real
# reads.  Both are imported lazily.
#
#   paho-mqtt>=1.6     # MQTT — subscribe to a reader's IoT Connector tag data
#   sllurp>=1.2.1      # LLRP — direct connection to an FX7500 / FX9600 / FXR90
#
# Install with:  pip install paho-mqtt        (recommended, via IoT Connector)
#            or:  pip install sllurp           (direct LLRP)
